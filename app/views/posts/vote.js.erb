<% if @vote.valid? %>
  $('#post_<%= @post.id %>_vote').html('<%= @post.total_votes %> vote<%= @post.total_votes == 1 ? " " : "s" %> ');
<% else %>
  alert("You can only vote for that post once!");
<% end %>